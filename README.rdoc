# Thanks for taking an interest in my little website!


#### Environment Variables
`DOMAIN=localhost:4200 # this is the default domian for the ember app on local machine`

### Local Setup

In one tab start Ember server
`$ ember s`

In another tab start the Rails server
`$ rails s`